# Прогресс разработки (Hron_GT1)

## Выполненные задачи:
- [x] Реализована математическая утилита `TradeMath` (`CORE/trade_math.py`):
  - `calculate_order_volume`: расчет количества актива в процентах от `invest_size` с использованием прецижнов из `spec_data`.
  - `calculate_take_profit_price`: расчет цен тейк-профитов на основе `avg_entry_price`.
- [x] Создан `LeverageManager` (`CORE/leverage_manager.py`) для кеширования и установки плеча и типа маржи.
- [x] Интеграция `margin_type` ("ISOLATED" по умолчанию) в рантайм-конфиги.
- [x] Добавлен `tp_map` со структурой `fallback_indent`.
- [x] Обновлен `TimeControl` (`CORE/shedjuler.py`) для работы с интервалами (в частности, `5m` из `app.json` -> `signal`).
- [x] В `PositionState` (FSM) добавлен флаг `in_position_papper` для соблюдения идемпотентности во время постановки первого ордера.
- [x] Обновлен главный торговый цикл в `CORE/bot.py`.

## В процессе / Следующие шаги:
- [ ] Фабрика создания объектов API клиента (Binance Futures), чтобы передавать `client` в `LeverageManager`.
- [ ] Окончательная привязка реальных рыночных цен.
- [ ] Обработка событий и полная связка с FSM для перевода стейтов из `in_position_papper` -> `in_position`.
- [ ] Реализация адаптеров для выставления ордеров (входа, усреднения, тейк-профитов и fallback-ордеров).
